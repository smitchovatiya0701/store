import React from "react";

const Friends = () => {
  return (
    <div>
      Friends
      {/* {totalPages > 1 && !isSpinner && (
    <Pagination
      currentPage={currentPage}
      setCurrentPage={setCurrentPage}
      totalPages={totalPages}
    />
  )} */}
    </div>
  );
};

export default Friends;
